# Copy these from Cipher's Netlify environment variables:
AZURE_STORAGE_ACCOUNT_NAME=carepathiqdata
AZURE_STORAGE_SAS_TOKEN=
VITE_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
HUBSPOT_CLIENT_ID=
HUBSPOT_CLIENT_SECRET=

# Do NOT copy from Cipher:
# ADMIN_USER_IDS — Cipher-specific
# APP_URL — will be a different URL for DQ
# HUBSPOT_REDIRECT_URI — DQ doesn't do OAuth
# CLERK_PUBLISHABLE_KEY — use VITE_ version only
